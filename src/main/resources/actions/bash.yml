triggerAmount: 8 # degrees (up-down)
windBackRotX: 30 # degrees (up-down)
attackRotX: 30 # degrees (up-down)
# if the attack speed converted to ticks is 12, the step per tick ends up being ceil(ticks * multiplier) / steps
speedMultiplier: 0.8
steps: 5 # how many collision checks
# see https://hub.spigotmc.org/javadocs/bukkit/org/bukkit/Material.html#enum-constant-summary
blacklist:
  - BOW
  - CROSSBOW